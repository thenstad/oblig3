#How to set the website up

- Copy/paste the code within the file dblogin.sql (SQL folder) to MySQL/MariaDB

- Edit dbconfig.php AND upload_item.php to match your database - 'host, dbname, user, password'
